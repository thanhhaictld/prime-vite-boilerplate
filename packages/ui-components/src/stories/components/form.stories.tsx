import { Meta } from "@storybook/react";

const meta: Meta = {
	title: "UI Kit / Forms",
	tags: ["autodocs"],
	//👇 Our exports that end in "Data" are not stories.
	excludeStories: /.*Data$/,
	args: {},
};


export default meta;